import { RegisterForm } from '@/components/auth/register-form';

export const metadata = {
  title: 'Cadastro - GranaFlow',
  description: 'Crie sua conta GranaFlow',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
