import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login - GranaFlow',
  description: 'Acesse sua conta GranaFlow',
};

export default function LoginPage() {
  return <LoginForm />;
}
